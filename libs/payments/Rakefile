# frozen_string_literal: true

require "rwm/rake"

cacheable_task :spec do
  sh "bundle exec rspec"
end

task :bootstrap do
  puts "Add bootstrap steps for payments here."
end

task :do_thing do
  puts "doing the thing in libs/payments"
end

task default: :spec
